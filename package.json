{
  "name": "campusvault-server",
  "version": "1.0.0",
  "description": "CampusVault CUIMS proxy — login + parse the Chandigarh University student portal and expose a JSON API for the mobile app.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "express": "^4.19.2"
  }
}
